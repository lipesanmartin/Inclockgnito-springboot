security:
  jwt:
    token:
      secret-key: kudu41r0
      expire-length: 3600000
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clock?useSSL=false
    username: root
    password: 2205
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
springdoc:
  pathsToMatch:
  - /api/**
  - /auth/**
  swagger-ui:
    use-root-path: true




